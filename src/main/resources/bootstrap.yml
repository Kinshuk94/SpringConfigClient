spring:
  application:
    name: s1rates
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: root
      fail-fast: true
      retry:
        max-attempts: 6
  profiles:
    active: qa
encrypt:
  key: ABCDEFGHIJKLMNOPQRSTUVWXYZ